<!DOCTYPE html>
<html>
  <title>Stripe Payment Demo</title>
  <body>
    <div style="display: flex; justify-content: center; align-items: center; height: 100vh;">
      <h2>Welcome to Payment Gateway</h2>
      <form action="paynow" method="POST">
        <script
          src="//checkout.stripe.com/v2/checkout.js"
          class="stripe-button"
          data-key="<%= key %>"
          data-amount="<%= Math.round(finaltotal)*100 %>"
          data-currency="inr"
          data-name="<SXC MERCH>"
          data-description="ORDER FROM SXC MERCH"
          data-image="/image/logo.svg"
          data-locale="auto"
        ></script>
      </form>
    </div>
  </body>
</html>
